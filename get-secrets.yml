- name: Get Secrets
  hosts: localhost
  vars:
    ssh: "{{lookup('hashi_vault', 'secret=common/data/ssh token={{vault_token}} url=https://vault.mohdshuja.com:8200 validate_certs=False')}}"
  tasks:
    - name: create secrets file
      ansible.builtin.lineinfile:
        path: secrets.json
        line: "{{ ssh }}"
        create: yes
